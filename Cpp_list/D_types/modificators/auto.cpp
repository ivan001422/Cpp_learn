/*
 * Ключевое слово: auto
 * Назначение: автоматический вывод типа переменной на основе инициализатора
 *             (не путать со старым значением «автоматическая продолжительность хранения»).
 * Применение: упрощает объявление сложных типов — итераторов, лямбд, шаблонных выражений.
 * Особенности: - Тип определяется компилятором во время компиляции.
 *              - Переменная должна быть инициализирована сразу.
 *              - Работает с указателями, ссылками, константами:
 *                auto, auto&, const auto&, auto*.
 *              - Не допускается для параметров функций (до C++20 — с ограничениями).
 */

#include <iostream>
#include <vector>
using namespace std;

int main() {
    auto x = 42;                 // int
    auto pi = 3.14159;           // double
    auto message = "Hello";      // const char*

    int value = 100;
    auto& ref = value;           // int&
    const auto& cref = value;    // const int&

    ref = 200;                   // меняет value
    cout << "value = " << value << ", cref = " << cref << '\n';

    // Используйте auto только в сложных типах данных, для базовых его применение избыточно.
    return 0;
}
