## Возвращение указателей из функций

Функция может возвращать указатель. Для этого её тип объявляется как `тип*`.

```cpp
#include <iostream>
using namespace std;

int* createInt(int value) {
    int* ptr = new int(value);   // динамическое выделение памяти
    return ptr;                  // указатель действителен после выхода из функции
}

Важно: возвращаемый указатель не должен ссылаться на локальную переменную функции, иначе он станет недействительным сразу после выхода из функции.

#include <iostream>
using namespace std;

int* dangerous() {
    int local = 10;
    return &local;   // ❌ ошибка: local будет уничтожена, указатель «повиснет»
}

Безопасно возвращать:

    адрес глобальной/статической переменной;
    память, выделенную через new;
    указатель, переданный в функцию через параметр.

