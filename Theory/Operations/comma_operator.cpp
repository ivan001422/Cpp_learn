#include <iostream>
using namespace std;

/*
 * Оператор «запятая» (comma operator): ,
 *
 * Назначение: выполняет два выражения последовательно (слева направо)
 * и возвращает значение правого выражения.
 *
 * Особенности:
 *  - Левое выражение вычисляется, его результат отбрасывается.
 *  - Правое выражение вычисляется и становится результатом всей операции.
 *  - Самый низкий приоритет среди всех операторов C++.
 *  - Часто используется в циклах for для нескольких инициализаций/обновлений.
 *
 * Важно: запятая в объявлениях переменных и списках аргументов функций
 * является разделителем, а не оператором.
 */

int main() {
    // ---------- Пример 1: оператор «запятая» в выражении ----------
    int a = (3, 5, 7);        // последовательно: 3, 5, 7; результат = 7
    cout << "a = (3, 5, 7) → a = " << a << endl;

    // ---------- Пример 2: присваивание с побочным эффектом ----------
    int x = 0, y = 0;
    // сначала x += 5 (x станет 5), потом y = x + 2 (y станет 7), вернётся y
    int result = (x += 5, y = x + 2);
    cout << "x = " << x << ", y = " << y << ", result = " << result << endl;

    // ---------- Пример 3: использование в цикле for ----------
    cout << "Цикл for с двумя счётчиками:\n";
    for (int i = 1, j = 10; i <= 5; ++i, --j) {
        cout << "i = " << i << ", j = " << j << endl;
    }

    // ---------- Важное отличие: запятая-разделитель ----------
    // Это НЕ оператор «запятая», а синтаксический разделитель:
    int p = 1, q = 2;          // объявление двух переменных
    func(1, 2, 3);             // вызов функции с тремя аргументами (здесь func не определена, для примера)

    return 0;
}
