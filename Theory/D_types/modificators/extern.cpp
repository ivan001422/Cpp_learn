/*
 * extern — объявление с внешним связыванием (переменная/функция определены в другом файле).
 * extern "C" — модификатор компоновки: имя функции не искажается (C-линковка),
 *              нужно для вызова C-функций из C++ или наоборот.
 */
#include <iostream>
using namespace std;

// Обычный extern (переменная будет определена ниже или в другом .cpp)
extern int shared_counter;

// extern "C" – C-линковка для функции
extern "C" void c_print(int val);

// Определение переменной
int shared_counter = 42;

// Определение функции с C-линковкой
extern "C" void c_print(int val) {
    cout << "c_print: " << val << endl;
}

int main() {
    cout << shared_counter << endl;   // 42
    shared_counter = 100;
    c_print(shared_counter);          // c_print: 100
    return 0;
}

/*
 * Объявление (declaration) сообщает компилятору о существовании имени и его типе,
 * но не выделяет память и не задаёт тело функции. Пример: extern int x;
 *
 * Определение (definition) не только объявляет имя, но и выделяет память (для переменных)
 * или содержит тело функции. Пример: int x = 42; или void func() { ... }
 *
 * В C++ действует "правило одного определения" (ODR): в программе может быть только одно
 * определение, но объявлений может быть несколько (если они согласованы).
 */
```
